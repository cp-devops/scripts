pipeline {
  agent any
  stages {
    stage('') {
      steps {
        sh 'echo "Hello World"
        sh '''
            echo "Multi-line shell steps work"
            ls -lah
            '''
      }
    }

  }
}
